---
description: Mute the text-to-speech voice output for this session
argument-hint: [--all]
disable-model-invocation: true
---

If the user passes `--all` as an argument, pass it through to mute every session globally.

Run this command:

```bash
claude-tts mute $ARGUMENTS
```

Summarize the output to the user.
